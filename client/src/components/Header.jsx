import './Header.css';

const Header = () => {
  return (
    <header className="header">
      <h2>Admin Dashboard</h2>
      {/* Add more header content as needed */}
    </header>
  );
};

export default Header;
